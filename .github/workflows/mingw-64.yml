name: mingw-64

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: recursive
      - run: sudo apt install gcc-mingw-w64-x86_64 g++-mingw-w64-x86_64
      - run: "./mingw64.sh"
